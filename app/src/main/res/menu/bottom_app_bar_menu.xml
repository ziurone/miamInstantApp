<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/dispensary"
        android:icon="@drawable/ic_fridge_small"
        android:title="@string/app_bottom_bar_menu_dispensary_title"
        android:enabled="true"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/doableRecipesList"
        android:icon="@drawable/ic_search_small"
        android:title="@string/app_bottom_bar_menu_recipe_book_title"
        android:enabled="true"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/recipeBookFragment"
        android:icon="@drawable/ic_recipebook_small"
        android:title="@string/app_bottom_bar_menu_recipebook_title"
        android:enabled="true"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/shopPurchaseFragment"
        android:icon="@drawable/ic_shopping_cart"
        android:title="@string/app_bottom_bar_menu_shopping_cart_title"
        app:showAsAction="ifRoom"/>

</menu>